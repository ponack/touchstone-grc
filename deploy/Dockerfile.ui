# syntax=docker/dockerfile:1.7

FROM node:22-alpine AS builder
WORKDIR /src
# Pin pnpm to v10 to match the lockfile format used by local dev + CI.
# Letting corepack pick "latest" pulls pnpm v11, which dropped the
# package.json `pnpm.overrides` field and refuses frozen-lockfile installs
# when overrides are present.
RUN corepack enable && corepack prepare pnpm@10 --activate
COPY ui/package.json ui/pnpm-lock.yaml* ./
RUN pnpm install --frozen-lockfile
COPY ui/ ./
RUN pnpm build

FROM node:22-alpine AS runtime
WORKDIR /app
COPY --from=builder /src/build ./build
COPY --from=builder /src/package.json ./package.json
COPY --from=builder /src/node_modules ./node_modules
ENV NODE_ENV=production
EXPOSE 3000
CMD ["node", "build"]
